
Pourquoi mon application plante ou appelle la fonction en boucle au chargement ? (on assume que la fonction supprimerJeu existe et qu'elle s'attend à recevoir un entier, et que jeu.id retourne bel et bien un int)

```tsx
<button onClick={supprimerJeu(jeu.id)}>Supprimer</button>
```
